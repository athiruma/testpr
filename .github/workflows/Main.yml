
name: Build

on:
  push:
    branches: [ main ]

jobs:
  job1:
  
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3
     - name: Cache
       uses: actions/cache@v3
       with:
         key: file.txt
         path: file.txt
     - name: test file
       run: |
         echo "Hello" > file.txt
         cat file.txt
  job2:
   runs-on: ubuntu-latest
   needs: job1
   steps:
     - uses: actions/checkout@v3
     - name: Cache
       uses: actions/cache@v3
       with:
         key: file.txt
         path: file.txt
     - name: Run
       run: |
         cat file.txt
				
				
