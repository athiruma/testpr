
name: Build

on:
  push:
    branches: [ main ]

jobs:
  job1:
  
    runs-on: ubuntu-latest
    name: job1
    steps:
     - uses: actions/checkout@v3
     - uses: actions/cache@v3
       with:
         path: ./file.txt
         key: file
     - name: Test hello
       run: |
         echo Hello > file.txt
         cat file.txt
